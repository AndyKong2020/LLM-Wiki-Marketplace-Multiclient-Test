#!/usr/bin/env bash
# Generated by scripts/sync_adapters.py; do not edit by hand.
set -euo pipefail

repo="https://github.com/AndyKong2020/LLM-Wiki-Marketplace-Multiclient-Test.git"
ref="main"
workdir="${TMPDIR:-/tmp}/llm-wiki-opencode-bootstrap.$$"

cleanup() {
  rm -rf "${workdir}"
}
trap cleanup EXIT

git clone --depth 1 --branch "${ref}" "${repo}" "${workdir}"
bash "${workdir}/dist/opencode/install-opencode.sh" "$@"
